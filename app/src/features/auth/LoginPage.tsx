// TODO: Implement login form — React Hook Form + Zod
// Spec: docs/auth-spec.md
// Command: src/api/auth.ts → login()
// Session: src/store/session.ts → setSession()
export default function LoginPage() {
  return <div>เข้าสู่ระบบ — placeholder</div>;
}
